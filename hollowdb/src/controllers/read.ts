import { RouteHandler } from "fastify";

export const get: RouteHandler = (request, reply) => {};

export const getMany: RouteHandler = () => {};

export const getRaw: RouteHandler = () => {};

export const getManyRaw: RouteHandler = () => {};
