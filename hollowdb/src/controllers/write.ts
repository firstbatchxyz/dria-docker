import { RouteHandler } from "fastify";

export const put: RouteHandler = (request, reply) => {};

export const putMany: RouteHandler = () => {};

export const update: RouteHandler = () => {};

export const remove: RouteHandler = () => {};
